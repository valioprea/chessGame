import javax.swing.*;

public class King extends Piece{
    public King(String name, Position initialPosition, Icon icon, GameLogic gameLogic, boolean isWhite, boolean firstMove) {
        super(name, initialPosition, icon, gameLogic, isWhite, firstMove);
    }
}
