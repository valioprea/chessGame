import javax.swing.*;

public class Bishop extends Piece {
    public Bishop(String name, Position initialPosition, Icon icon, GameLogic gameLogic, boolean isWhite, boolean firstMove) {
        super(name, initialPosition, icon, gameLogic, isWhite, firstMove);
    }
}
